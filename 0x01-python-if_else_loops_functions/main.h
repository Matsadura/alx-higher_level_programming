#ifndef MAIN
#define MAIN
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
def islower(c):
	def uppercase(str):
		def print_last_digit(number):
			def add(a, b):
				def pow(a, b):
					def fizzbuzz():
						listint_t *insert_node(listint_t **head, int number);
						int _putchar(char c);
#endif /* MAIN */
